<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="<%=home_path%>">Home</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav">
      <li class="nav-item">
        <a class="nav-link" href="<%=team_path%>">Equipe</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="<%=contact_path%>">Contact</a>
      </li>
    </ul>
  </div>
</nav>
<h1>Bienvenue sur The Gossip Project !</h1>
<% @gossips.each do |gossip| %>
  <p>
  <div class="card" style="width: 18rem;">
  <div class="card-body">
    <h5 class="card-title"> <%= gossip.title %> </h5>
    <h6 class="card-subtitle mb-2 text-muted">créer par : <a href="<%= user_path(gossip.user.id) %>" > <%= gossip.user.first_name %> </a></h6>
    <a href="<%= potin_path(gossip.id) %>" > Voir plus </a>
  </div>
  </div>
  </p>
<% end %>
<p>
  <a href="<%=new_gossip_path%>">Clique ici pour un nouveau gossip</a>